import Link from "next/link";
import { dashboardCounts, listAdminPatterns } from "@/lib/admin-data";
export default async function RakeDashboard() { const [counts, recent] = await Promise.all([dashboardCounts(), listAdminPatterns()]); const values = [["Total Patterns",counts.total],["Published",counts.published],["Draft",counts.draft],["Hidden",counts.hidden],["Removed",counts.removed],["Rights Review",counts.review]]; return <><h2>Dashboard</h2><div className="rake-grid">{values.map(([label,value])=><div className="rake-stat" key={label as string}><span>{label}</span><b>{value as number}</b></div>)}</div><section className="rake-panel"><div className="rake-actions"><h3>Recent changes</h3><Link className="rake-button" href="/rake/patterns/new">Upload pattern</Link></div><table className="rake-table"><thead><tr><th>Title</th><th>Status</th><th>Rights</th><th>Updated</th></tr></thead><tbody>{recent.slice(0,10).map((pattern)=><tr key={pattern.id}><td><Link href={`/rake/patterns/${pattern.id}`}>{pattern.title}</Link></td><td>{pattern.status}</td><td>{pattern.rights_status}</td><td>{pattern.updated_at}</td></tr>)}</tbody></table></section></>; }
