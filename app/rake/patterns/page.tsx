import Link from "next/link";
import { listAdminPatterns } from "@/lib/admin-data";
export default async function AdminPatterns() { const patterns=await listAdminPatterns(); return <><div className="rake-actions"><h2>Patterns</h2><Link className="rake-button" href="/rake/patterns/new">Upload</Link></div><section className="rake-panel"><table className="rake-table"><thead><tr><th>Preview</th><th>Title / slug</th><th>Category</th><th>Status</th><th>Rights</th><th>Created</th><th>Updated</th></tr></thead><tbody>{patterns.map((pattern)=><tr key={pattern.id}><td>{pattern.preview_url && <img className="rake-thumb" src={pattern.preview_url} alt="" />}</td><td><Link href={`/rake/patterns/${pattern.id}`}>{pattern.title}</Link><br/><small>{pattern.slug}</small></td><td>{pattern.category_name ?? "—"}</td><td><span className="rake-status">{pattern.status}</span></td><td>{pattern.rights_status}</td><td>{pattern.created_at}</td><td>{pattern.updated_at}</td></tr>)}</tbody></table></section></>; }
